export const purchaseStart = () => {
    return (dispatch) => (
        type: 'PURCHASE_START',
        purchaseProcess
    )
}

export const purchaseEnd = () => {
    return (dispatch) => (
        type: 'PURCHASE_END',
        purchaseProcess
    )
}
